#ifndef AREA_H
#define AREA_H

double calculateArea(double radius);                      // Circle
double calculateArea(double length, double breadth);      // Rectangle
double calculateArea(double base, double height, bool);  // Triangle (bool just to differentiate signature)

#endif
